return {
    init=function(P)
    end,
    
    title=function(P)
        return "Green Meadows"
    end,

    rulesText=function(P)
        return {
            "The classic setting. No changes.",
        }
    end,
}
